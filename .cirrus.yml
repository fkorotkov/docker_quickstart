docker_builder:
  name: Docker Build
  build_script: docker build .

task:
  name: Docker (Kaniko) Build
  container:
    image: gcr.io/kaniko-project/executor:debug
  env:
    CIRRUS_SHELL: busybox
  build_script: executor --tarPath=/tmp/image.tar --context=$CIRRUS_WORKING_DIR

