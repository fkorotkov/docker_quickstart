task:
  name: Docker (Kaniko) Build
  container:
    image: gcr.io/kaniko-project/executor:debug
  build_script: executor --destination=/tmp/image.tar --tarPath=/tmp/image.tar --context=$CIRRUS_WORKING_DIR

